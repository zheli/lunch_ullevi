'use strict';

/**
 * @ngdoc service
 * @name lunchUlleviApp.ullevigatanFactory
 * @description
 * # ullevigatanService
 * Service in the lunchUlleviApp.
 */
angular.module('lunchUlleviApp', ['ngResource'])
  .factory('ullevigatanMenu', ['$resource',
    function ($resource) {
        // AngularJS will instantiate a singleton by calling "new" on this function
        return;
  }]);
